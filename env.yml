name: pykumu
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - ipykernel
  - jupyterlab
  - numpy
  - pandas
  - scipy
  - jpype1
  - python-igraph
  # Uses system JDK (Corretto 21) via JAVA_HOME/PATH
  - pip:
      - pyvis
